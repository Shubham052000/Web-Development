//Refer to MDN docs for more Array functions
// .map() will help you to run a function over elements of an array
const numbers = [1, 2, 3];

const doubleNumArray = numbers.map((num) => num * 2);

console.log(numbers);
console.log(doubleNumArray);
