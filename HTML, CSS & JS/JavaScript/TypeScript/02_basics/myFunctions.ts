function addTwo(num: number): number {
  return num + 2;
}

let myValue = addTwo(5);

const loginUser = (name: string, email: string, isPaid: true) => {};

const fruits = ["apple", "banana", "cherry", "dragonfruit", "etrog"];
// const fruits = [1, 2, 3, 4, 5];

fruits.map((fruit) => `fruit is ${fruit}`);
